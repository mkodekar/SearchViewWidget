package com.mlapsoftware.materilsearchview.deprecated;

/*import android.content.res.Configuration;
import android.content.res.TypedArray;
import android.os.Bundle;
import android.support.v4.widget.DrawerLayout;
import android.support.v7.app.ActionBarDrawerToggle;
import android.support.v7.app.AppCompatActivity;
import android.support.v7.widget.DefaultItemAnimator;
import android.support.v7.widget.LinearLayoutManager;
import android.support.v7.widget.RecyclerView;
import android.support.v7.widget.Toolbar;
import android.view.View;

import com.melnykov.fab.FloatingActionButton;
import com.mlapsoftware.flathoc.R;
import com.mlapsoftware.flathoc.ui.Old.NavDrawerAdapter;
import com.mlapsoftware.flathoc.ui.Old.NavDrawerItem;

import java.util.ArrayList;*/


//public class NavigationViewActivity2 extends AppCompatActivity {


  /*  private void setupNavDrawer() {
    private NavDrawerAdapter mNavDrawerAdapter;
     //   RecyclerView mDrawerRecycleView = (RecyclerView) findViewById(R.id.navdrawer_);
     //   TypedArray navMenuIcons = getResources().obtainTypedArray(R.array.navdrawer_icons);
      //  String[] navMenuTitles = getResources().getStringArray(R.array.navdrawer_titles);

        ArrayList<NavDrawerItem> mNavDrawerList = new ArrayList<>();

        for (int i = 0; i < 4; i++) {
            if (i == 1) {
                mNavDrawerList.add(new NavDrawerItem(navMenuIcons
                        .getResourceId(i, -1), navMenuTitles[i], true));
            } else {
                mNavDrawerList.add(new NavDrawerItem(navMenuIcons
                        .getResourceId(i, -1), navMenuTitles[i], false));
            }
        }
        navMenuIcons.recycle();

        mNavDrawerAdapter = new NavDrawerAdapter(this, mNavDrawerList);
        mNavDrawerAdapter.setOnItemClickListener(new NavDrawerAdapter.OnItemClickListener() {
            @Override
            public void onItemClick(View view, int position) {
                view.setSelected(true);
                //selectItem(position);
            }
        });

        mDrawerRecycleView.setHasFixedSize(true);
        mDrawerRecycleView.setLayoutManager(new LinearLayoutManager(this));
        mDrawerRecycleView.setItemAnimator(new DefaultItemAnimator());
        mDrawerRecycleView.setAdapter(mNavDrawerAdapter);

        mNavDrawerAdapter.notifyDataSetChanged();
    }
*/
//}